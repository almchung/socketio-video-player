var playing = false;
var videos;

function setup() {
  //createCanvas(1920, 1080);
  // specify multiple formats for different browsers
  //videos = createVideo("/assets/sample.mov");
  //videos.hide();
}

function draw(){
  //background(150);
  //image(videos,0,0);
}

// plays or pauses the video depending on current state
/*function toggleVid() {
  if (playing) {
    videos.pause();
  } else {
    videos.loop();
  }
  playing = !playing;
}*/

function playVid(){
  //videos.loop();
  console.log("VIDEO PLAYING!!!!");
}